# Backend Setup для Gmail SMTP

Цей гайд допоможе налаштувати відправку email з контактної форми через Gmail SMTP.

## 📋 Передумови

1. Gmail акаунт
2. Node.js встановлений на комп'ютері
3. Двоетапна перевірка увімкнена в Google Account

## 🔧 Крок 1: Налаштування Gmail

### 1.1. Увімкни двоетапну перевірку

1. Перейди на [myaccount.google.com/security](https://myaccount.google.com/security)
2. Знайди розділ "Як ви входите в Google"
3. Натисни "Двоетапна перевірка"
4. Увімкни її, дотримуючись інструкцій

### 1.2. Створи пароль додатка (App Password)

1. У налаштуваннях безпеки знайди "Паролі додатків" (App Passwords)
2. Виберіть:
   - **Додаток**: Пошта (Mail)
   - **Пристрій**: Інший (вказати назву) → введи "Ways 2 Spain Backend"
3. Натисни "Згенерувати"
4. **ВАЖЛИВО**: Скопіюй згенерований 16-символьний пароль (формат: `xxxx xxxx xxxx xxxx`)
   - Цей пароль показується лише один раз!
   - Зберігай його в безпечному місці

## 📦 Крок 2: Встановлення залежностей

```bash
npm install
```

Це встановить всі необхідні пакети:
- `express` - веб-сервер
- `nodemailer` - для відправки email
- `dotenv` - для змінних оточення
- `cors` - для дозволу запитів з фронтенду
- `concurrently` - для одночасного запуску фронтенду та бекенду

## ⚙️ Крок 3: Налаштування .env файлу

1. Скопіюй файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

2. Відкрий `.env` та заповни:

```env
GMAIL_USER=ways2spain@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
RECIPIENT_EMAIL=ways2spain@gmail.com
PORT=3001
```

**ВАЖЛИВО:**
- `GMAIL_USER` - твій Gmail адрес
- `GMAIL_APP_PASSWORD` - 16-символьний пароль, який ти створив (можна з пробілами або без)
- `RECIPIENT_EMAIL` - куди надсилати заявки (зазвичай той самий email)
- `PORT` - порт бекенд сервера (3001 за замовчуванням)

## 🚀 Крок 4: Запуск серверів

### Варіант A: Запустити все разом

```bash
npm run dev:all
```

Це запустить одночасно:
- Фронтенд на `http://localhost:8080`
- Бекенд на `http://localhost:3001`

### Варіант B: Запустити окремо

**Термінал 1** (фронтенд):
```bash
npm run dev
```

**Термінал 2** (бекенд):
```bash
npm run dev:server
```

## 🧪 Крок 5: Перевірка роботи

1. Відкрий сайт: `http://localhost:8080/contact`
2. Заповни форму
3. Надішли заявку
4. Перевір email - має прийти лист з даними форми

### Перевірка бекенду

Перевір, чи працює сервер:
```bash
curl http://localhost:3001/health
```

Має повернутися:
```json
{"status":"ok","message":"Server is running"}
```

## 📁 Структура файлів

```
dn-consulting-landing/
├── server/
│   ├── index.js          # Головний файл сервера
│   └── routes/
│       └── contact.js    # Роут для обробки форми
├── .env                  # Змінні оточення (НЕ комітити в Git!)
├── .env.example          # Приклад конфігурації
└── package.json          # Залежності та скрипти
```

## 🔒 Безпека

1. **НІКОЛИ не коміть `.env` файл в Git!**
   - Він вже в `.gitignore`
   - Зберігай паролі в безпеці

2. **Пароль додатка ≠ Пароль від Gmail**
   - Пароль додатка створюється окремо
   - Якщо забув - створи новий

3. **Обмеження Gmail**
   - До 500 листів на день
   - До ~100 листів на годину

## 🐛 Розв'язання проблем

### Помилка "EAUTH"
- Перевір правильність `GMAIL_APP_PASSWORD`
- Переконайся, що створив пароль додатка, а не використовуєш звичайний пароль

### Помилка "ECONNECTION"
- Перевір інтернет-з'єднання
- Можливо, Gmail блокує підключення - перевір налаштування безпеки

### Email не надходить
- Перевір `RECIPIENT_EMAIL` в `.env`
- Перевір папку "Спам" в Gmail
- Переглянь логи сервера в терміналі

### CORS помилка
- Переконайся, що бекенд запущений на порту 3001
- Перевір, що `cors` middleware підключений

## 📧 Формат email

Кожна заявка надсилається у форматі HTML з:
- Ім'ям та контактами клієнта
- Вибраною послугою та кейсом
- Повідомленням
- Часом отримання

## 🌐 Деплой на продакшн

Для деплою на Vercel/Netlify потрібно:
1. Додати змінні оточення в налаштуваннях хостингу
2. Налаштувати Vercel Serverless Functions або окремий бекенд хостинг

## 💡 Поради

- Протестуй спочатку локально
- Перевіряй логи сервера при проблемах
- Зберігай `.env` файл в безпеці
- Регулярно перевіряй обмеження Gmail

---

**Питання?** Звертайся, допоможу налаштувати! 🚀

