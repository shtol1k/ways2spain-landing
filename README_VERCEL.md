# Налаштування бекенду на Vercel

Цей гайд покаже, як налаштувати відправку email через Gmail SMTP на Vercel.

## 🎯 Що змінилося?

Замість локального Express сервера тепер використовуються **Vercel Serverless Functions**:
- ✅ Працює без окремого сервера
- ✅ Автоматично масштабується
- ✅ Працює на всіх версіях сайту (dev, preview, production)
- ✅ Безкоштовний план Vercel достатній для початку

## 📁 Структура файлів

```
dn-consulting-landing/
├── api/
│   └── contact.js          # Vercel Serverless Function
├── server/                 # (залишається для локальної розробки)
│   ├── index.js
│   └── routes/
├── src/
│   └── pages/
│       └── Contact.tsx     # Форма (автоматично визначає URL)
└── vercel.json             # Конфігурація Vercel
```

## 🚀 Крок 1: Налаштування на Vercel

### 1.1. Підключи проєкт до Vercel

Якщо ще не підключено:
1. Перейди на [vercel.com](https://vercel.com)
2. Увійди через GitHub
3. Натисни "Add New Project"
4. Вибери свій репозиторій
5. Vercel автоматично визначить налаштування

### 1.2. Додай Environment Variables

1. У проєкті на Vercel перейди в **Settings** → **Environment Variables**
2. Додай наступні змінні:

| Name | Value | Environment |
|------|-------|-------------|
| `GMAIL_USER` | `ways2spain@gmail.com` | Production, Preview, Development |
| `GMAIL_APP_PASSWORD` | `xxxx xxxx xxxx xxxx` | Production, Preview, Development |
| `RECIPIENT_EMAIL` | `ways2spain@gmail.com` | Production, Preview, Development |

**Важливо:**
- `GMAIL_APP_PASSWORD` - це 16-символьний пароль додатка (App Password) з Google
- Додай для всіх середовищ (Production, Preview, Development)
- Після додавання потрібен redeploy проєкту

### 1.3. Як отримати Gmail App Password?

Якщо ще не маєш:
1. Перейди на [myaccount.google.com/security](https://myaccount.google.com/security)
2. Увімкни "Двоетапна перевірка" (якщо ще не увімкнена)
3. Знайди "Паролі додатків" (App Passwords)
4. Створи новий: **Додаток** → Пошта, **Пристрій** → Ways 2 Spain Vercel
5. Скопіюй 16-символьний пароль

## 📦 Крок 2: Деплой

### Автоматичний деплой

Після push в GitHub:
```bash
git add .
git commit -m "Add Vercel serverless function for contact form"
git push
```

Vercel автоматично:
- Задеплоїть нову версію
- Запустить build
- Активує serverless function

### Перевірка деплою

1. Відкрий свій сайт на Vercel (наприклад: `https://ways2spain.vercel.app`)
2. Перейди на сторінку `/contact`
3. Заповни форму і надішли
4. Перевір email - має прийти лист

## 🔧 Локальна розробка

Для локальної розробки можна використовувати обидва варіанти:

### Варіант A: Локальний Express сервер (як раніше)

```bash
npm run dev:all
```

### Варіант B: Vercel CLI для тестування serverless функцій

```bash
# Встанови Vercel CLI
npm i -g vercel

# Запусти локально з Vercel
vercel dev
```

## 🌐 Як працює на продакшн?

```
Користувач заповнює форму
    ↓
POST /api/contact
    ↓
Vercel Serverless Function (api/contact.js)
    ↓
Nodemailer через Gmail SMTP
    ↓
Email приходить на RECIPIENT_EMAIL
```

## 📝 Перевірка роботи

### 1. Перевірка через Vercel Dashboard

1. Перейди в проєкт на Vercel
2. Відкрий вкладку **Functions**
3. Подивись логи викликів `/api/contact`

### 2. Перевірка через браузер

Відкрий DevTools → Network → спробуй відправити форму → подивись на запит до `/api/contact`

### 3. Перевірка email

- Перевір папку "Вхідні"
- Перевір папку "Спам" (якщо не прийшло)
- Перевір, що `RECIPIENT_EMAIL` правильно налаштовано

## 🐛 Розв'язання проблем

### Помилка 500 при відправці

**Можливі причини:**
1. Не налаштовані Environment Variables
   - **Рішення:** Перевір Settings → Environment Variables на Vercel
2. Неправильний Gmail App Password
   - **Рішення:** Створи новий пароль додатка в Google Account
3. Не зроблено redeploy після додавання змінних
   - **Рішення:** Зроби redeploy проєкту на Vercel

**Як перевірити:**
- Відкрий Vercel Dashboard → Functions → `/api/contact` → Logs
- Там побачиш детальні помилки

### CORS помилка

На Vercel CORS налаштовується автоматично. Якщо є проблема:
- Перевір, що форма використовує правильний URL
- У коді вже є автоматичне визначення: локально → localhost, на продакшн → `/api/contact`

### Email не надходить

1. **Перевір Environment Variables:**
   - `GMAIL_USER` встановлено?
   - `GMAIL_APP_PASSWORD` правильний?
   - `RECIPIENT_EMAIL` встановлено?

2. **Перевір логи Vercel:**
   - Dashboard → Functions → `/api/contact` → Logs
   - Шукай помилки типу `EAUTH`, `ECONNECTION`

3. **Перевір папку "Спам"** в Gmail

## 💰 Вартість

**Vercel Hobby Plan (безкоштовно):**
- ✅ 100 GB bandwidth на місяць
- ✅ Serverless Functions: 100GB-hours на місяць
- ✅ Безмежні деплої
- ✅ Автоматичні SSL сертифікати

**Для твого проєкту цього більш ніж достатньо!**

Якщо буде > 100 заявок на день (що дуже багато для початку), можна:
- Перейти на Pro план ($20/місяць)
- Або використати EmailJS (200 листів/місяць безкоштовно)

## 🔄 Міграція з локального сервера

Якщо вже використовував локальний сервер:

1. ✅ Код форми вже оновлено - він автоматично визначає URL
2. ✅ Локальний сервер залишився для розробки (`server/` папка)
3. ✅ Додай Environment Variables на Vercel
4. ✅ Зроби redeploy

## 📊 Моніторинг

Vercel надає базовий моніторинг:
- **Functions** → `/api/contact` → побачиш кількість викликів
- **Logs** → детальні логи кожного виклику
- **Analytics** → статистика використання (на Pro плані)

## ✅ Чек-лист перед деплоєм

- [ ] Gmail двоетапна перевірка увімкнена
- [ ] Створено Gmail App Password
- [ ] Додано Environment Variables на Vercel:
  - [ ] `GMAIL_USER`
  - [ ] `GMAIL_APP_PASSWORD`
  - [ ] `RECIPIENT_EMAIL`
- [ ] Зроблено redeploy проєкту
- [ ] Протестовано форму на продакшн

## 🎉 Готово!

Після налаштування форма буде працювати автоматично на всіх версіях сайту:
- Production (твій домен)
- Preview (якщо використовуєш preview деплої)
- Development (якщо використовуєш Vercel CLI)

**Питання?** Звертайся! 🚀

