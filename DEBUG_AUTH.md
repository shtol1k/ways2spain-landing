# 🔍 Діагностика проблем з авторизацією

Якщо після деплою сторінка авторизації показує білий екран, використай цю інструкцію для діагностики.

## 🚀 Швидка перевірка

### 1. Перевір консоль браузера

1. Відкрий сайт в браузері
2. Натисни `F12` або `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
3. Перейди на вкладку **Console**
4. Шукай помилки (червоні повідомлення)

**Що шукати:**
- ❌ `Failed to fetch` - проблема з API
- ❌ `Module not found` - проблема з імпортами
- ❌ `SyntaxError` - помилка в коді
- ❌ `TypeError` - проблема з типами

### 2. Перевір Network (Мережа)

1. В консолі браузера перейди на вкладку **Network**
2. Онови сторінку (`F5`)
3. Перевір чи завантажуються файли:
   - `index.html` ✅
   - JavaScript файли (`*.js`) ✅
   - CSS файли (`*.css`) ✅

**Що перевіряти:**
- Якщо файли не завантажуються (404) - проблема з build/deploy
- Якщо файли завантажуються, але є помилки - проблема з кодом

### 3. Перевір API endpoints

Відкрий в браузері або через curl:

```bash
# Перевірка діагностичного endpoint
https://твій-сайт.vercel.app/api/auth/debug

# Перевірка verify endpoint
https://твій-сайт.vercel.app/api/auth/verify
```

**Очікуваний результат:**
- `debug` endpoint повинен повернути JSON з інформацією про конфігурацію
- `verify` endpoint повинен повернути `{"success": false, "authenticated": false}` якщо не авторизований

## 🔧 Детальна діагностика

### Перевірка 1: Environment Variables

1. Перейди в Vercel Dashboard → Settings → Environment Variables
2. Перевір чи додані:
   - `AUTH_USERNAME` ✅
   - `AUTH_PASSWORD` ✅
   - `AUTH_TOKEN` (опціонально)
   - `AUTH_SECRET` (опціонально)

3. Переконайся що вони додані для **Preview** та **Development** (НЕ Production!)

### Перевірка 2: Middleware

Перевір чи middleware працює:

1. Відкрий DevTools → Network
2. Відкрий будь-яку сторінку (не `/login`)
3. Перевір Response Headers:
   - Якщо є `Location: /login` - middleware працює ✅
   - Якщо ні - middleware не спрацював

### Перевірка 3: React Errors

1. Відкрий DevTools → Console
2. Шукай помилки React:
   - `Error: Minified React error` - помилка в компоненті
   - `Warning: Invalid hook call` - проблема з хуками

3. Якщо є помилки - Error Boundary має показати дружнє повідомлення замість білого екрану

### Перевірка 4: API Response

1. Відкрий DevTools → Network
2. Знайди запит до `/api/auth/verify`
3. Перевір:
   - Status Code: має бути `200` або `401`
   - Response: має бути JSON
   - Headers: перевір чи є `Content-Type: application/json`

## 🐛 Типові проблеми та рішення

### Проблема: Білий екран, немає помилок в консолі

**Можливі причини:**
1. CSS не завантажився
2. JavaScript не виконався
3. React не змонтувався

**Рішення:**
1. Перевір Network tab - чи завантажуються файли
2. Перевір Console - чи є попередження
3. Спробуй hard refresh: `Cmd+Shift+R` (Mac) / `Ctrl+Shift+R` (Windows)

### Проблема: "Failed to fetch" в консолі

**Можливі причини:**
1. API endpoint не працює
2. CORS проблема
3. Network проблема

**Рішення:**
1. Перевір `/api/auth/debug` в браузері
2. Перевір чи API routes деплоїлися правильно
3. Перевір Environment Variables в Vercel

### Проблема: Сторінка завантажується, але форма не працює

**Можливі причини:**
1. JavaScript помилка в компоненті
2. Проблема з хуками (useToast, useNavigate)

**Рішення:**
1. Перевір Console на помилки
2. Перевір чи всі компоненти імпортуються правильно
3. Перевір чи всі залежності встановлені

### Проблема: Авторизація працює, але редірект не спрацьовує

**Можливі причини:**
1. Проблема з React Router
2. Проблема з navigate функцією

**Рішення:**
1. Перевір Console на помилки
2. Перевір чи `useNavigate` працює
3. Перевір чи route `/` існує

## 📋 Чеклист для швидкої перевірки

- [ ] Environment Variables додані в Vercel
- [ ] Environment Variables додані для Preview/Development (НЕ Production)
- [ ] Зроблено redeploy після додавання змінних
- [ ] API endpoint `/api/auth/debug` працює
- [ ] Немає помилок в Console браузера
- [ ] JavaScript файли завантажуються (Network tab)
- [ ] CSS файли завантажуються (Network tab)
- [ ] Error Boundary показує помилки (якщо вони є)

## 🔗 Корисні посилання

- **Vercel Dashboard**: https://vercel.com/dashboard
- **Vercel Logs**: В Dashboard → Deployments → [твій deployment] → Logs
- **Browser DevTools**: F12 або Cmd+Option+I

## 📞 Як зібрати інформацію для підтримки

Якщо проблема не вирішується, збери наступну інформацію:

1. **Console logs**: Скопіюй всі помилки з Console
2. **Network requests**: Скріншот Network tab з помилками
3. **API response**: Результат `/api/auth/debug` endpoint
4. **Vercel logs**: Логи з Vercel Dashboard
5. **Environment**: Який енвайронмент (Preview/Development)
6. **Browser**: Який браузер використовуєш

