# 🚀 ШВИДДКЕ ВИПРАВЛЕННЯ ВІДГУКІВ

## 🔴 Проблема

Frontend показує "Не вдалося завантажити відгуки", тому що Strapi API повертає `403 Forbidden`.

## ✅ Рішення (5 хвилин)

### КРОК 1: Створити API Token в Strapi (2 хв)

1. **Відкрий адмін панель:**
   - Local: http://localhost:1337/admin

2. **Перейди в:**
   - Settings (зубastaсте колесо зліва) → **API Tokens** (розділ "Administration")

3. **Створи новий токен:**
   - Натисни кнопку **"Create new API Token"**
   - Name: `frontend-public-token`
   - Description: `Token for frontend to read testimonials`
   - Token duration: ✅ **Unlimited**
   - Token type: ✅ **Read-only** (非常重要!)
   - Натисни **"Generate"**

4. **Скопіюй токен:**
   - Він виглядає як: `ProductionToken-xxxxxxxxxx` або інший формат
   - Збереж його - він показується лише один раз!

### КРОК 2: Додати токен в .env.local (1 хв)

Відкрий файл `frontend/.env.local` і заміни рядок:

```bash
VITE_STRAPI_API_TOKEN=your-api-token-here
```

на:

```bash
VITE_STRAPI_API_TOKEN=ProductionToken-твій-реальний-токен-тут
```

### КРОК 3: Перезавантажити frontend (1 хв)

```bash
# Зупинити frontend (Ctrl+C)
# Потім знову запустити:
cd frontend
npm run dev
```

### КРОК 4: Перевірити (1 хв)

1. Відкрий http://localhost:8080
2. Перейди до блоку "Відгуки наших клієнтів"
3. Ти маєш побачити свої 5 локальних відгуків! 🎉

---

## 🌍 Production (Vercel)

Після того, як працює локально:

1. **Повтори КРОК 1** для production Strapi:
   - Відкрий: https://твій-strapi-app.onrender.com/admin
   - Створи новий API token (можеш з такою ж назвою)
   - Скопіюй токен

2. **Додай в Vercel:**
   - Vercel Dashboard → твій проект → Settings → Environment Variables
   - Додай змінну:
     ```
     VITE_STRAPI_API_TOKEN = твій-production-токен
     ```
     ```
     VITE_STRAPI_URL = https://твій-strapi-app.onrender.com
     ```

3. **Редеплой:**
   - Пуш в develop → автодеплой
   - Готово! ✅

---

## 🧪 Тестування API

Можеш перевірити API прямо в терміналі:

```bash
# Без токена (повинно повернути 403):
curl http://localhost:1337/api/testimonials

# З токеном (повинно повернути дані):
curl -H "Authorization: Bearer ProductionToken-твій-токен" \
  "http://localhost:1337/api/testimonials?populate=Photo"
```

---

## 📋 Checklist

- [ ] Створив API токен в local Strapi
- [ ] Додав токен в `frontend/.env.local`
- [ ] Перезавантажив frontend
- [ ] Бачу відгуки на localhost:8080 ✅
- [ ] Створив API токен в production Strapi
- [ ] Додав токен в Vercel Environment Variables
- [ ] Задеплоїв і перевірив на production site

---

## 💡 Пояснення

**Чому це необхідно?**
- Strapi v5 захищає API за замовчуванням
- Public access вимкнений для безпеки
- API tokens дозволяють читати дані без повної авторизації

**Чому Read-only?**
- Frontend тільки читає відгуки
- Не може змінювати/видаляти
- Це безпечніше для production

---

## 🔥 Швидко працює!

Після налаштування:
- ✅ Local → Local PostgreSQL
- ✅ Production → Supabase
- ✅ Автоматично оновлюється
- ✅ Безпечно та надійно
