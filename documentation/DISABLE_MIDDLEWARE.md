# 🔧 Тимчасове вимкнення middleware

## Поточний стан

- Активний файл `middleware.ts` лише реекспортує логіку з `middleware/index.ts`.
- Усі тимчасові копії (`middleware.ts.backup*`) видалені, щоб уникнути плутанини під час деплою.

## Як тимчасово вимкнути middleware

1. Перейменуй файл-реекспортер:
   ```bash
   mv middleware.ts middleware.disabled.ts
   ```
2. Виконай `git add` та за потреби зафіксуй коміт.
3. Задеплой або перезапусти локальний сервер — middleware не буде виконуватись.

> Альтернатива для локального тесту: відкрий `middleware.ts` і закоментуй рядок `export { config, default } from './middleware/index';`.

## Як повернути middleware

1. Поверни назву файлу:
   ```bash
   mv middleware.disabled.ts middleware.ts
   ```
2. Переконайся, що Vercel отримав актуальний репозиторій (redeploy).

## Корисні підказки

- Вся бізнес-логіка знаходиться в `middleware/index.ts`, тому зміни варто робити саме там.
- Щоб вимкнути middleware лише для частини маршрутів, можна тимчасово змінити `config.matcher`.
- Тримай структуру без резервних копій у git — це спрощує роботу з репозиторієм і деплой.

